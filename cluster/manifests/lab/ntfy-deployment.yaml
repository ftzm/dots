apiVersion: apps/v1
kind: Deployment
metadata:
  name: ntfy
  namespace: ntfy
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: ntfy
  strategy:
    type: Recreate
  template:
    metadata:
      annotations:
        checksum/config: 40758efc1757f25fe0a1e8f64646ce01
      labels:
        app.kubernetes.io/name: ntfy
    spec:
      containers:
      - args:
        - serve
        image: binwiederhier/ntfy
        name: ntfy
        ports:
        - containerPort: 80
          name: http
        volumeMounts:
        - mountPath: /etc/ntfy
          name: config
        - mountPath: /etc/ntfy/templates
          name: templates
        - mountPath: /var/cache/ntfy
          name: cache
      volumes:
      - configMap:
          name: ntfy-config
        name: config
      - configMap:
          name: ntfy-templates
        name: templates
      - name: cache
        persistentVolumeClaim:
          claimName: ntfy-cache
