apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: healthchecks-config
  namespace: healthchecks
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: nfs
