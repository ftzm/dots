apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: jellyfin
  namespace: default
spec:
  entryPoints:
  - web
  - websecure
  routes:
  - kind: Rule
    match: Host(`jellyfin.ftzmlab.xyz`)
    services:
    - name: jellyfin
      port: 8096
  tls: {}
