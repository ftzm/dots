apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: ntfy
  namespace: ntfy
spec:
  entryPoints:
  - privateweb
  - privatesecure
  routes:
  - kind: Rule
    match: Host(`ntfy.lan.ftzmlab.xyz`)
    services:
    - name: ntfy
      port: 80
  tls: {}
