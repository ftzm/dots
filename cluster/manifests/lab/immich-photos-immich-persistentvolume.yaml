apiVersion: v1
kind: PersistentVolume
metadata:
  name: immich-photos-immich
spec:
  accessModes:
  - ReadWriteMany
  capacity:
    storage: 500Gi
  nfs:
    path: /pool-1/cloud/photos
    server: 192.168.1.3
  persistentVolumeReclaimPolicy: Retain
  storageClassName: immich-photos-immich
