apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/name: webdav
  name: webdav
  namespace: default
spec:
  clusterIP: None
  ports:
  - name: http
    port: 8080
    protocol: TCP
    targetPort: 8080
