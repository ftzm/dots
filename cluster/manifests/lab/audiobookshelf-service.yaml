apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/name: audiobookshelf
  name: audiobookshelf
  namespace: default
spec:
  clusterIP: None
  ports:
  - name: http
    port: 8000
    protocol: TCP
    targetPort: 8000
