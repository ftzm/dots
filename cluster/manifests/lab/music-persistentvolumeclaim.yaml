apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: music
  namespace: navidrome
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 500Gi
  storageClassName: music-navidrome
  volumeName: music-navidrome
