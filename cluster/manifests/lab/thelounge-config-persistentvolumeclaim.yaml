apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: thelounge-config
  namespace: thelounge
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: nfs
