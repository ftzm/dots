apiVersion: traefik.io/v1alpha1
kind: IngressRouteTCP
metadata:
  name: argocd
  namespace: argocd
spec:
  entryPoints:
  - privatesecure
  routes:
  - match: HostSNI(`argo.lan.ftzmlab.xyz`)
    services:
    - name: argocd-server
      port: 443
  tls:
    passthrough: true
